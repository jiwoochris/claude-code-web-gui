# Use a real node_modules tree so Next.js + node-pty + any tooling that
# assumes npm-style resolution works out of the box. PnP is stricter but
# not worth the blast radius here.
nodeLinker: node-modules

# Keep yarn's compressed package cache out of the working tree;
# Docker builds will just re-download on fresh installs.
enableGlobalCache: true
